DATABASE_URL="postgresql://seeky:seeky@localhost:5432/seeky?schema=public"
PORT=3001
# Candidate region polygons + (fallback) employer autocomplete.
# Leave empty to use the built-in offline fallback cities (no network/key needed).
GEOAPIFY_API_KEY=b8568cb9afc64fad861a69edbddb2658

# Employer location autocomplete via Google Places API (legacy) + Place Details.
# Requires "Places API" enabled on the key. Empty => falls back to Geoapify.
GOOGLE_MAPS_API_KEY=AIzaSyALzBNL8R5jTIhlOnjAcOaIotUEvuptsUk
